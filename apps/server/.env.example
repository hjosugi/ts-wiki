# Server port
PORT=4000

# SQLite database file (created on first run)
DATABASE_PATH=./data/wiki.sqlite

# Directory for runtime data (uploaded assets, etc.)
DATA_DIR=./data

# Secret used to sign auth tokens — CHANGE THIS in production.
JWT_SECRET=dev-insecure-secret-change-me

# ── Git storage (optional; mirrors pages to a Git repo, like Wiki.js) ────────
# When enabled, every page change writes a Markdown file + commit (DB → Git),
# and POST /api/git/sync imports external commits (Git → DB).
# (Namespaced WIKI_GIT_* on purpose — plain GIT_DIR/GIT_AUTHOR_* are reserved by Git.)
WIKI_GIT_ENABLED=false
# Repo working directory (defaults to <DATA_DIR>/repo).
# WIKI_GIT_DIR=./data/repo
WIKI_GIT_BRANCH=main
# Optional remote to pull from (your own cron drives pushes).
# WIKI_GIT_REMOTE=origin
WIKI_GIT_AUTHOR_NAME=open-wiki
WIKI_GIT_AUTHOR_EMAIL=wiki@localhost
