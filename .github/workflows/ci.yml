name: CI

on:
  push:
  pull_request:

jobs:
  verify:
    name: Install, typecheck, test, and build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.14

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Lint
        run: bun run lint

      - name: Typecheck
        run: bun run typecheck

      - name: Test
        run: bun run test

      - name: Build web
        run: bun run build

      - name: Install Chromium
        run: bunx playwright install chromium --with-deps

      - name: Browser smoke test
        run: bun run test:e2e

      - name: Dependency audit
        run: bun audit --production

  container:
    name: Build and smoke-test container
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Build image
        run: docker build -t kawaii-wiki-ci .

      - name: Start image
        run: |
          docker run -d --name kawaii-wiki-ci -p 4000:4000 \
            -v kawaii-wiki-ci-data:/data \
            kawaii-wiki-ci

      - name: Smoke health and setup
        run: |
          for attempt in {1..30}; do
            if curl --fail --silent http://127.0.0.1:4000/api/health >/dev/null; then break; fi
            if [ "$attempt" = 30 ]; then docker logs kawaii-wiki-ci; exit 1; fi
            sleep 1
          done
          curl --fail --silent http://127.0.0.1:4000/api/setup/status | grep 'needsSetup'

      - name: Show logs on failure
        if: failure()
        run: docker logs kawaii-wiki-ci

      - name: Stop image
        if: always()
        run: |
          docker rm -f kawaii-wiki-ci || true
          docker volume rm kawaii-wiki-ci-data || true

  external-database-contract:
    name: Provision and test external libSQL
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.14

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Provision external database and run contracts
        run: bash scripts/test-external-database.sh

  postgres-contract:
    name: Provision and test PostgreSQL
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:17-alpine
        env:
          POSTGRES_USER: wiki
          POSTGRES_PASSWORD: wiki
          POSTGRES_DB: wiki
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -h 127.0.0.1 -U wiki -d wiki"
          --health-interval 2s
          --health-timeout 3s
          --health-retries 30

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.14

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Run PostgreSQL contract
        env:
          KAWAII_WIKI_TEST_POSTGRES_URL: postgres://wiki:wiki@127.0.0.1:5432/wiki
        run: bun test apps/server/src/db/postgres

  mysql-contract:
    name: Provision and test MySQL
    runs-on: ubuntu-latest

    services:
      mysql:
        image: mysql:8
        env:
          MYSQL_ROOT_PASSWORD: wiki
          MYSQL_DATABASE: wiki
          MYSQL_USER: wiki
          MYSQL_PASSWORD: wiki
        ports:
          - 3306:3306
        options: >-
          --health-cmd "mysqladmin ping -h 127.0.0.1 -u wiki -pwiki --silent"
          --health-interval 2s
          --health-timeout 3s
          --health-retries 40

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.14

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Run MySQL contract
        env:
          # Root: the contract harness creates/drops a database per test file.
          KAWAII_WIKI_TEST_MYSQL_URL: mysql://root:wiki@127.0.0.1:3306/wiki
        # One file at a time: each provisions its own 35-table database, so
        # running them together serialises MySQL DDL and starves per-file setup.
        run: |
          for file in apps/server/src/db/mysql/*.test.ts; do
            echo "-- $file --"
            bun test "$file"
          done

  elasticsearch-contract:
    name: Provision and test Elasticsearch
    runs-on: ubuntu-latest

    services:
      elasticsearch:
        image: docker.elastic.co/elasticsearch/elasticsearch:8.15.0
        env:
          discovery.type: single-node
          xpack.security.enabled: "false"
          ES_JAVA_OPTS: "-Xms512m -Xmx512m"
        ports:
          - 9200:9200
        options: >-
          --health-cmd "curl -fsS http://127.0.0.1:9200/_cluster/health || exit 1"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 40

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.14

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Run Elasticsearch contract
        env:
          KAWAII_WIKI_TEST_ELASTICSEARCH_URL: http://127.0.0.1:9200
        run: bun test apps/server/src/search/elasticsearch
